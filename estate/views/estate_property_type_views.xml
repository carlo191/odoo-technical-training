<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- FORM VIEW -->
    <record id="estate_property_type_view_form" model="ir.ui.view">
        <field name="name">estate.property.type.view.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button type="object"
                                name="action_open_offer_ids"
                                class="oe_stat_button"
                                icon="fa-money">
                            <field name="offer_count" string="Offerte"/>
                        </button>
                    </div>
                    <h1>
                        <field name="name" placeholder="Tipo di Proprieta"/>
                    </h1>
                    <notebook>
                        <page string="Proprieta">
                            <field name="property_ids">
                                <list>
                                    <field name="name" string="Titolo"/>
                                    <field name="expected_price" string="Prezzo Previsto"/>
                                    <field name="selling_price" string="Prezzo Vendita"/>
                                    <field name="state" string="Stato" widget="badge"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- LIST VIEW -->
    <record id="estate_property_type_view_list" model="ir.ui.view">
        <field name="name">estate.property.type.view.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" string="Nome"/>
            </list>
        </field>
    </record>
</odoo>
